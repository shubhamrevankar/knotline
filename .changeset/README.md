# Changesets

Add a changeset with `pnpm changeset` when a change alters the behavior or public API of a
versioned workspace package. Private application-only and infrastructure changes do not require one.
